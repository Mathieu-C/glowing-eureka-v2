import styled from "styled-components";

const Wrapper = styled.div`
  margin: 0 auto;
  max-width: 580px;
  padding: 12px;
  position: relative;
`;

const Styles = {
  Wrapper,
};

export default Styles;
